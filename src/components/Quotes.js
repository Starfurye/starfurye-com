import React from "react";

// 必须
export const Must = ({ message }) => {};

// 建议
export const Suggestion = ({ message }) => {};

// 禁止
export const Danger = ({ message }) => {};

// 警告
export const Warning = ({ message }) => {};

// 提示
export const Note = ({ message }) => {};

// 引用或背景
export const Quote = ({ message }) => {};
